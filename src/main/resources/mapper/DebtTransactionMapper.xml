<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cheongsan.domain.debt.mapper.DebtTransactionMapper">

    <insert id="insertDebtTransaction" parameterType="cheongsan.domain.debt.entity.DebtTransaction">
        INSERT INTO debt_transactions (debt_account_id, transaction_date, principal_amount,
                                       interest_amount, remaining_balance, created_at)
        VALUES (#{debtAccountId}, #{transactionDate}, #{principalAmount},
                #{interestAmount}, #{remainingBalance}, #{createdAt})
    </insert>

    <select id="findByDebtAccountId" parameterType="long" resultType="cheongsan.domain.debt.entity.DebtTransaction">
        SELECT *
        FROM debt_transactions
        WHERE debt_account_id = #{debtAccountId}
        ORDER BY transaction_date DESC
    </select>

    <select id="isDebtTransactionExists" resultType="java.lang.Boolean">
        SELECT EXISTS (SELECT 1
                       FROM debt_transactions
                       WHERE debt_account_id = #{debtAccountId}
                         AND transaction_date = #{transactionDate})
    </select>

    <select id="findByDebtAccountIdAndDateRange" resultType="cheongsan.domain.debt.entity.DebtTransaction">
        SELECT *
        FROM debt_transactions
        WHERE debt_account_id = #{debtAccountId}
          AND transaction_date BETWEEN #{startDate} AND #{endDate}
        ORDER BY transaction_date DESC
    </select>
</mapper>