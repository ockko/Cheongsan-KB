<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cheongsan.domain.notification.mapper.NotificationMapper">
    <select id="findNotificationByUserId" parameterType="long"
            resultType="cheongsan.domain.notification.entity.Notification">
        SELECT id, user_id, contents, type, is_read, created_at
        FROM notifications
        WHERE user_id = #{userId}
          AND is_read = 0
        ORDER BY created_at DESC
    </select>

    <select id="countUnreadNotificationByUserId" parameterType="long" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM notifications
        WHERE user_id = #{userId}
          AND is_read = 0
    </select>

    <select id="countTodayNotificationsByType" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM notifications
        WHERE user_id = #{userId}
          AND type = #{type}
          AND DATE(created_at) = CURDATE()
    </select>

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notifications (user_id, contents, type, is_read, created_at)
        VALUES (#{userId}, #{contents}, #{type}, #{isRead}, #{createdAt})
    </insert>

    <update id="markNotificationAsRead">
        UPDATE notifications
        SET is_read = 1
        WHERE user_id = #{userId}
          AND is_read = 0
    </update>

    <insert id="insertNotification">
        INSERT INTO notifications (user_id, contents, type, is_read, created_at)
        VALUES (#{userId}, #{contents}, #{type}, false, CURRENT_TIMESTAMP)
    </insert>

</mapper>