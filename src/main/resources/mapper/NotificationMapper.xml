<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cheongsan.domain.notification.mapper.NotificationMapper">
    <insert id="insertNotification" parameterType="cheongsan.domain.notification.entity.NotificationEntity"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notifications (user_id, contents, is_read)
        VALUES (#{userId}, #{contents}, #{isRead})
    </insert>

    <select id="findNotificationByUserId" parameterType="long"
            resultType="cheongsan.domain.notification.entity.NotificationEntity">
        SELECT id, user_id, contents, is_read, created_at
        FROM notifications
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <select id="countUnreadNotificationByUserId" parameterType="long" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM notifications
        WHERE user_id = #{userId}
          AND is_read = 0
    </select>

    <update id="markNotificationAsRead">
        UPDATE notifications
        SET is_read = 1
        WHERE id = #{id}
    </update>
</mapper>