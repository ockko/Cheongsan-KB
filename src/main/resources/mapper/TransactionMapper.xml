<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cheongsan.domain.notification.mapper.TransactionMapper">
    <insert id="insertTransaction" parameterType="cheongsan.domain.notification.entity.Transactions">
        INSERT INTO transactions (deposit_account_id, user_id, transaction_time, after_balance, amount, type)
        VALUES (#{depositAccountId}, #{userId}, #{transactionTime}, #{afterBalance}, #{amount}, #{type})
    </insert>

</mapper>